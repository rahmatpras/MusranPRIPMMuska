<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simamat.musranpripmmuska">

    //internet permission
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_musran_ipm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_musran_ipm"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DaftarPemilihActivity" />
        <activity android:name=".HomeAdminActivity" />
        <activity android:name=".SignInAdminActivity" />
        <activity
            android:name=".PopActivity"
            android:theme="@style/AppTheme.PopMe" />
        <activity android:name=".PilihanActivity" />
        <activity android:name=".IsiDataPemilihActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>